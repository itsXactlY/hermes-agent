name: mazemaker
version: 1.1.0
description: "Mazemaker — local semantic memory with knowledge graph, spreading activation, auto-connections, and dream consolidation. Runs entirely offline."
requires_env: []
hooks:
  - on_session_end
  - on_memory_write
